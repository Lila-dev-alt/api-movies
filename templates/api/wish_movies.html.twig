{% extends 'base.html.twig' %}
{% block body %}
    {{ parent() }}
<h1>Wish list de Films : </h1>
<br>
{% for wish_movie in wish_movies %}
    {{ wish_movie.id }}.
    {{ wish_movie.name }}
    <a href="{{ path('delete_movie', {'id': wish_movie.id}) }}">Supprimer</a>
    <br>
{% endfor %}

<h1>listes d'acteurs : </h1>
<br>
{% for actor in actors %}
{{ actor.id }}.
{{ actor.name}}
 id :   {{ actor.idTheMovieDb }}
    <a class="btn btn-primary" href="{{ path('delete_actor', {'id': actor.id}) }}">Supprimer</a>
    <br>
{% endfor %}

{% endblock %}
